<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="commentRatingBar" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/ratingbar_comment_drawable</item>
        <item name="android:minHeight">20dip</item>
    </style>
    <style name="btn_style_green">
        <item name="android:background">@drawable/selector_btn</item>
        <item name="android:layout_marginLeft">@dimen/dimens_5dp</item>
        <item name="android:layout_marginRight">@dimen/dimens_5dp</item>
        <item name="android:textSize">@dimen/text_common_size</item>
    </style>
</resources>
